<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">#FFFFFF</item>
    </style>

    <!-- Style of EditText box title -->
    <style name="styleEditTextTitle">
        <item name="android:layout_marginLeft">8dp</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textColor">@color/colorDarkText</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Style of a mandatory EditText box -->
    <style name="styleMandatoryEditTextBox">
        <item name="android:background">@color/colorBackgroundLight</item>
        <item name="android:textColorHint">@color/colorAccent</item>
        <item name="android:editTextColor">@color/colorDarkText</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Style of an additional info EditText box -->
    <style name="styleAdditionalEditTextBox">
        <item name="android:background">@color/colorBackgroundDark</item>
        <item name="android:textColorHint">@color/colorAccent</item>
        <item name="android:editTextColor">@color/colorDarkText</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- Style of Spinners -->
    <style name="styleSpinner">
        <item name="android:background">@color/colorBackgroundDark</item>
        <item name="android:editTextColor">@color/colorDarkText</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

</resources>
